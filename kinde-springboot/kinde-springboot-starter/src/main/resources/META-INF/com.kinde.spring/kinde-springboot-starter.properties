# Minimal marker resource so the otherwise-empty starter jar contains at least
# one entry. Without it, maven-jar-plugin emits "JAR will be empty" every build.
# This module is a transitive-dependency aggregator only; no code lives here.
artifact=kinde-springboot-starter
